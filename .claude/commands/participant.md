Focus on the Participant portal only. Read the following docs before proceeding with any task:

@docs/roles/participant/overview.md
@docs/roles/participant/food-selection.md
@docs/shared/sensitive-areas.md

You are working in the Participant portal (`src/pages/participant/`). Do not load or reason about Admin, Booker, or Partner internals unless directly called from participant code paths.
